(function(){'use strict';const g=globalThis;g.ModalOverlay=class{static show(html,onConfirm,title='',confirmText='Save'){const overlay=document.createElement('div');overlay.className='modal-overlay';overlay.innerHTML=`<div class="modal-content"><div class="modal-handle"></div>${title?`<h2>${title}</h2>`:''}${html}<div style="display:flex;gap:10px;margin-top:16px"><button class="btn btn-outline btn-block btn-sm" id="modalCancel">Cancel</button><button class="btn btn-primary btn-block btn-sm" id="modalConfirm">${confirmText}</button></div></div>`;document.body.appendChild(overlay);overlay.querySelector('#modalCancel').addEventListener('click',()=>overlay.remove());overlay.querySelector('#modalConfirm').addEventListener('click',()=>{if(onConfirm)onConfirm();if(overlay.parentNode)overlay.remove()});overlay.addEventListener('click',(e)=>{if(e.target===overlay)overlay.remove()});return overlay}};})();
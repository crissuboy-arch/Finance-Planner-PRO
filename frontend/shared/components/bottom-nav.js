(function(){'use strict';const g=globalThis;const NAV_ITEMS=[{id:'home',icon:'🏠',label:'Home'},{id:'ai-advisor',icon:'🤖',label:'AI'},{id:'calendar',icon:'📅',label:'Calendar'},{id:'budget',icon:'📊',label:'Budget'},{id:'profile',icon:'👤',label:'Profile'}];function renderNav(){const nav=document.getElementById('bottomNav');if(!nav)return;nav.innerHTML=NAV_ITEMS.map(item=>`<button class="nav-item" data-screen="${item.id}"><span class="nav-icon">${item.icon}</span><span class="nav-label">${item.label}</span></button>`).join('');nav.querySelectorAll('.nav-item').forEach(btn=>{btn.addEventListener('click',()=>{if(g.navigate)g.navigate(btn.dataset.screen)})})}document.addEventListener('DOMContentLoaded',renderNav);g.updateNav=renderNav;})();
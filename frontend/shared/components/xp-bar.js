(function(){'use strict';const g=globalThis;g.renderXPBar=function(container){const state=g.FPState;const pct=Math.min(100,(state.xp/state.xpToNext)*100);const el=document.createElement('div');el.className='xp-bar';el.innerHTML=`<span class="xp-icon">⭐</span><div class="xp-info"><div class="xp-level">Level ${state.user.level}</div><div class="xp-progress">${state.xp} / ${state.xpToNext} XP</div><div class="progress-bar xp-progress-bar"><div class="progress-fill" style="width:${pct}%"></div></div></div>`;container.appendChild(el)};g.updateGamificationUI=function(){document.querySelectorAll('.xp-bar-dynamic').forEach(el=>{const state=g.FPState;const pct=Math.min(100,(state.xp/state.xpToNext)*100);el.innerHTML=`<span class="xp-icon">⭐</span><div class="xp-info"><div class="xp-level">Level ${state.user.level}</div><div class="xp-progress">${state.xp} / ${state.xpToNext} XP</div><div class="progress-bar xp-progress-bar"><div class="progress-fill" style="width:${pct}%"></div></div></div>`})}})();
(function(){'use strict';const g=globalThis;const BUDGET_CATS=[{icon:'🍔',name:'Food & Dining'},{icon:'🛒',name:'Groceries'},{icon:'🚗',name:'Transportation'},{icon:'🏠',name:'Housing'},{icon:'💡',name:'Utilities'},{icon:'🎬',name:'Entertainment'},{icon:'🛍️',name:'Shopping'},{icon:'🏥',name:'Healthcare'},{icon:'📚',name:'Education'},{icon:'📌',name:'Other'}];g.FPScreens.budget={init:function(container,{onNavigate}){let view='monthly';render();function render(){const state=g.FPState;const budgets=state.budgets||[];const now=new Date();let periodBudgets;let periodExpenses;if(view==='weekly'){const weekStart=new Date(now);weekStart.setDate(now.getDate()-now.getDay());periodBudgets=budgets.filter(b=>b.period==='weekly'||b.period==='monthly');periodExpenses=state.transactions.filter(e=>{const ed=new Date(e.date);return ed>=weekStart})}else if(view==='daily'){periodBudgets=budgets.filter(b=>b.period==='daily'||b.period==='monthly');periodExpenses=state.transactions.filter(e=>{const ed=new Date(e.date);return ed.toDateString()===now.toDateString()})}else{const monthStart=new Date(now.getFullYear(),now.getMonth(),1);periodBudgets=budgets;periodExpenses=state.transactions.filter(e=>{const ed=new Date(e.date);return ed>=monthStart})}const totalBudget=periodBudgets.reduce((s,b)=>s+Number(b.amount),0);const totalSpent=periodExpenses.reduce((s,e)=>s+Number(e.amount),0);const savings=state.totalSavings||0;const monthlyIncome=state.income.filter(i=>{const d=new Date(i.date);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear()}).reduce((s,i)=>s+Number(i.amount),0);container.innerHTML=`<div class="screen active"><div class="screen-header"><h1>Budget</h1><button class="btn-icon" onclick="navigate('home')">✕</button></div><div class="screen-body"><div class="tab-bar"><div class="tab-item ${view==='daily'?'active':''}" data-view="daily">Daily</div><div class="tab-item ${view==='weekly'?'active':''}" data-view="weekly">Weekly</div><div class="tab-item ${view==='monthly'?'active':''}" data-view="monthly">Monthly</div></div><div class="card card-gradient"><div class="card-header"><h3 style="color:white">${view.charAt(0).toUpperCase()+view.slice(1)} Budget</h3></div><div class="amount" style="color:white">${g.formatCurrency(totalSpent)}</div><div style="font-size:13px;opacity:0.8;margin-top:2px">of ${g.formatCurrency(totalBudget||monthlyIncome)} budget</div><div class="progress-bar" style="margin-top:10px;background:rgba(255,255,255,0.2)"><div class="progress-fill ${totalBudget>0&&totalSpent>totalBudget?'danger':''}" style="width:${totalBudget>0?Math.min(100,(totalSpent/totalBudget)*100):0}%"></div></div></div><div class="stat-row"><div class="stat-card"><div class="stat-icon">💰</div><div class="stat-value positive">${g.formatCurrency(monthlyIncome)}</div><div class="stat-label">Income</div></div><div class="stat-card"><div class="stat-icon">🏦</div><div class="stat-value positive">${g.formatCurrency(savings)}</div><div class="stat-label">Savings</div></div></div><button class="btn btn-primary btn-block" id="addBudgetBtn">+ Set Budget Limit</button><div style="height:8px"></div><div class="section-title"><span>Budget Categories</span></div><div id="budgetList">${budgets.length===0?`<div class="empty-state"><div class="empty-icon">📊</div><h3>No budgets set</h3><p>Create budget limits for each category to track spending</p></div>`:budgets.slice().reverse().map(b=>{const cat=BUDGET_CATS.find(c=>c.name===b.category);const spent=(state.transactions||[]).filter(e=>{const catMatch=!b.category||e.category===b.category.toLowerCase().replace(' & ','').replace(' ','');const ed=new Date(e.date);const now2=new Date();return ed.getMonth()===now2.getMonth()&&ed.getFullYear()===now2.getFullYear()}).reduce((s,e)=>s+Number(e.amount),0);const pct=b.amount>0?Math.min(100,(spent/b.amount)*100):0;return`<div class="budget-card"><div class="budget-category"><div><span>${cat?cat.icon:'📌'}</span><span class="budget-cat-name"> ${b.category}</span></div><div class="budget-spent ${spent>b.amount?'over':''}">${g.formatCurrency(spent)}</div></div><div class="progress-bar"><div class="progress-fill ${spent>b.amount?'danger':pct>80?'warning':''}" style="width:${pct}%"></div></div><div class="budget-detail">${g.formatCurrency(spent)} of ${g.formatCurrency(b.amount)} · ${Math.round(pct)}% used</div></div>`}).join('')}</div></div>`;container.querySelectorAll('.tab-item').forEach(el=>{el.addEventListener('click',()=>{view=el.dataset.view;render()})});document.getElementById('addBudgetBtn').addEventListener('click',showAddBudget)}function showAddBudget(){const html=`<div class="input-group"><label>Category</label><div class="input-wrap"><select id="modalCategory">${BUDGET_CATS.map(c=>`<option value="${c.name}">${c.icon} ${c.name}</option>`).join('')}</select></div></div><div class="input-group"><label>Limit Amount ($)</label><div class="input-wrap"><input type="number" id="modalAmount" placeholder="0.00" step="0.01" min="0"></div></div><div class="input-group"><label>Period</label><div class="input-wrap"><select id="modalPeriod"><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly" selected>Monthly</option></select></div></div>`;g.showModal(html,()=>{const cat=document.getElementById('modalCategory').value;const amount=document.getElementById('modalAmount').value;const period=document.getElementById('modalPeriod').value;if(!amount)return;const state=g.FPState;const existing=state.budgets.findIndex(b=>b.category===cat);if(existing>=0){state.budgets[existing].amount=Number(amount);state.budgets[existing].period=period}else{state.budgets.push({id:g.uid(),category:cat,amount:Number(amount),period})}g.saveState();g.addXP(5);render();g.showToast('Budget set! +5 XP','success')},'Set Budget')}}};})();
(function(){'use strict';const g=globalThis;const TYPES=[{icon:'🏦',name:'Loan'},{icon:'💳',name:'Credit Card'},{icon:'🏠',name:'Mortgage'},{icon:'👤',name:'Personal'},{icon:'📄',name:'Installment'},{icon:'📌',name:'Other'}];g.FPScreens['debt-tracker']={init:function(container,{onNavigate}){render();function render(){const state=g.FPState;const debts=state.debts||[];const total=debts.reduce((s,d)=>s+Number(d.amount),0);const remaining=debts.reduce((s,d)=>s+Number(d.remaining||d.amount),0);const pct=total>0?Math.round(((total-remaining)/total)*100):0;container.innerHTML=`<div class="screen active"><div class="screen-header"><h1>Debt Tracker</h1><button class="btn-icon" onclick="navigate('home')">✕</button></div><div class="screen-body"><div class="card card-gradient"><div class="card-header"><h3 style="color:white">Total Debt</h3></div><div class="amount" style="color:white">${g.formatCurrency(remaining)}</div><div class="progress-bar" style="margin-top:10px;background:rgba(255,255,255,0.2)"><div class="progress-fill" style="width:${pct}%;background:rgba(255,255,255,0.6)"></div></div><div style="font-size:13px;opacity:0.8;margin-top:6px">${pct}% paid off · ${g.formatCurrency(total-remaining)} of ${g.formatCurrency(total)}</div></div><button class="btn btn-primary btn-block" id="addDebtBtn">+ Add Debt</button><div style="height:4px"></div><div class="section-title"><span>Your Debts</span></div><div id="debtList">${debts.length===0?`<div class="empty-state"><div class="empty-icon">💳</div><h3>No debts tracked</h3><p>Add your debts to track payoff progress</p></div>`:debts.slice().reverse().map(d=>{const t=TYPES.find(t=>t.name===d.type);const dpct=d.amount>0?Math.round(((d.amount-(d.remaining||d.amount))/d.amount)*100):0;return`<div class="debt-card"><div class="debt-header"><div><span style="font-size:18px">${t?t.icon:'📌'}</span><span class="debt-name"> ${d.name}</span></div><div class="debt-amount">${g.formatCurrency(d.remaining||d.amount)}</div></div><div class="debt-details">${d.type}${d.interest?` · ${d.interest}% interest`:''}${d.dueDate?` · Due: ${d.dueDate}`:''}</div><div class="progress-bar debt-progress"><div class="progress-fill ${dpct>75?'':dpct>50?'warning':'danger'}" style="width:${dpct}%"></div></div><div style="display:flex;justify-content:space-between;margin-top:4px;font-size:12px;color:var(--text-muted)"><span>${g.formatCurrency(d.amount-(d.remaining||d.amount))} paid</span><span>${dpct}%</span></div><div style="display:flex;gap:8px;margin-top:10px"><button class="btn btn-primary btn-sm" style="flex:1" data-debt-pay="${d.id}">Make Payment</button></div></div>`}).join('')}</div></div>`;document.getElementById('addDebtBtn').addEventListener('click',showAddDebt);container.querySelectorAll('[data-debt-pay]').forEach(btn=>{btn.addEventListener('click',()=>{showPayment(btn.dataset.debtPay)})})}function showAddDebt(){const html=`<div class="input-group"><label>Debt Name</label><div class="input-wrap"><input type="text" id="modalName" placeholder="e.g. Student Loan"></div></div><div class="input-group"><label>Total Amount ($)</label><div class="input-wrap"><input type="number" id="modalAmount" placeholder="0.00" step="0.01" min="0"></div></div><div class="input-group"><label>Type</label><div class="input-wrap"><select id="modalType">${TYPES.map(t=>`<option value="${t.name}">${t.icon} ${t.name}</option>`).join('')}</select></div></div><div class="input-group"><label>Interest Rate (%)</label><div class="input-wrap"><input type="number" id="modalInterest" placeholder="0" step="0.1" min="0"></div></div><div class="input-group"><label>Due Date</label><div class="input-wrap"><input type="date" id="modalDueDate"></div></div>`;g.showModal(html,()=>{const name=document.getElementById('modalName').value;const amount=document.getElementById('modalAmount').value;const type=document.getElementById('modalType').value;const interest=document.getElementById('modalInterest').value;const dueDate=document.getElementById('modalDueDate').value;if(!name||!amount)return;const state=g.FPState;state.debts.push({id:g.uid(),name,amount:Number(amount),remaining:Number(amount),type,interest:interest||0,dueDate,created:new Date().toISOString()});g.saveState();g.addXP(5);render();g.showToast('Debt added! +5 XP','success')},'Add Debt')}function showPayment(debtId){const debt=state.debts.find(d=>d.id===debtId);if(!debt)return;const html=`<div class="input-group"><label>Payment Amount ($)</label><div class="input-wrap"><input type="number" id="modalPaymentAmount" placeholder="0.00" step="0.01" min="0" max="${debt.remaining}"></div></div><p style="font-size:13px;color:var(--text-muted)">Remaining balance: ${g.formatCurrency(debt.remaining)}</p>`;g.showModal(html,()=>{const payment=Number(document.getElementById('modalPaymentAmount').value);if(!payment||payment<=0)return;debt.remaining=Math.max(0,debt.remaining-payment);if(debt.remaining<=0){g.showToast(`🎉 Debt paid off: ${debt.name}`,'success');g.launchConfetti();g.addXP(50)}else{g.addXP(Math.round(payment/10));g.showToast(`Payment of ${g.formatCurrency(payment)} recorded!`,'success')}g.saveState();render()},`Pay ${debt.name}`,'Pay')}}};})();